/*
 * Generated by gdbus-codegen 2.34.1. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "org.gnome.Identity.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.gnome.Identity.Manager
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:GoaIdentityServiceManager
 * @title: GoaIdentityServiceManager
 * @short_description: Generated C code for the org.gnome.Identity.Manager D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.gnome.Identity.Manager ---- */

static const _ExtendedGDBusArgInfo _goa_identity_service_manager_method_info_exchange_secret_keys_IN_ARG_input_key =
{
  {
    -1,
    (gchar *) "input_key",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _goa_identity_service_manager_method_info_exchange_secret_keys_IN_ARG_pointers[] =
{
  &_goa_identity_service_manager_method_info_exchange_secret_keys_IN_ARG_input_key,
  NULL
};

static const _ExtendedGDBusArgInfo _goa_identity_service_manager_method_info_exchange_secret_keys_OUT_ARG_output_key =
{
  {
    -1,
    (gchar *) "output_key",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _goa_identity_service_manager_method_info_exchange_secret_keys_OUT_ARG_pointers[] =
{
  &_goa_identity_service_manager_method_info_exchange_secret_keys_OUT_ARG_output_key,
  NULL
};

static const _ExtendedGDBusMethodInfo _goa_identity_service_manager_method_info_exchange_secret_keys =
{
  {
    -1,
    (gchar *) "ExchangeSecretKeys",
    (GDBusArgInfo **) &_goa_identity_service_manager_method_info_exchange_secret_keys_IN_ARG_pointers,
    (GDBusArgInfo **) &_goa_identity_service_manager_method_info_exchange_secret_keys_OUT_ARG_pointers,
    NULL
  },
  "handle-exchange-secret-keys",
  FALSE
};

static const _ExtendedGDBusArgInfo _goa_identity_service_manager_method_info_sign_in_IN_ARG_identifier =
{
  {
    -1,
    (gchar *) "identifier",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _goa_identity_service_manager_method_info_sign_in_IN_ARG_details =
{
  {
    -1,
    (gchar *) "details",
    (gchar *) "a{ss}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _goa_identity_service_manager_method_info_sign_in_IN_ARG_pointers[] =
{
  &_goa_identity_service_manager_method_info_sign_in_IN_ARG_identifier,
  &_goa_identity_service_manager_method_info_sign_in_IN_ARG_details,
  NULL
};

static const _ExtendedGDBusArgInfo _goa_identity_service_manager_method_info_sign_in_OUT_ARG_identity_object_path =
{
  {
    -1,
    (gchar *) "identity_object_path",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _goa_identity_service_manager_method_info_sign_in_OUT_ARG_pointers[] =
{
  &_goa_identity_service_manager_method_info_sign_in_OUT_ARG_identity_object_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _goa_identity_service_manager_method_info_sign_in =
{
  {
    -1,
    (gchar *) "SignIn",
    (GDBusArgInfo **) &_goa_identity_service_manager_method_info_sign_in_IN_ARG_pointers,
    (GDBusArgInfo **) &_goa_identity_service_manager_method_info_sign_in_OUT_ARG_pointers,
    NULL
  },
  "handle-sign-in",
  FALSE
};

static const _ExtendedGDBusArgInfo _goa_identity_service_manager_method_info_sign_out_IN_ARG_identity =
{
  {
    -1,
    (gchar *) "identity",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _goa_identity_service_manager_method_info_sign_out_IN_ARG_pointers[] =
{
  &_goa_identity_service_manager_method_info_sign_out_IN_ARG_identity,
  NULL
};

static const _ExtendedGDBusMethodInfo _goa_identity_service_manager_method_info_sign_out =
{
  {
    -1,
    (gchar *) "SignOut",
    (GDBusArgInfo **) &_goa_identity_service_manager_method_info_sign_out_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-sign-out",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _goa_identity_service_manager_method_info_pointers[] =
{
  &_goa_identity_service_manager_method_info_exchange_secret_keys,
  &_goa_identity_service_manager_method_info_sign_in,
  &_goa_identity_service_manager_method_info_sign_out,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _goa_identity_service_manager_interface_info =
{
  {
    -1,
    (gchar *) "org.gnome.Identity.Manager",
    (GDBusMethodInfo **) &_goa_identity_service_manager_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "manager",
};


/**
 * goa_identity_service_manager_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
goa_identity_service_manager_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_goa_identity_service_manager_interface_info.parent_struct;
}

/**
 * goa_identity_service_manager_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #GoaIdentityServiceManager interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
goa_identity_service_manager_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * GoaIdentityServiceManager:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link>.
 */

/**
 * GoaIdentityServiceManagerIface:
 * @parent_iface: The parent interface.
 * @handle_exchange_secret_keys: Handler for the #GoaIdentityServiceManager::handle-exchange-secret-keys signal.
 * @handle_sign_in: Handler for the #GoaIdentityServiceManager::handle-sign-in signal.
 * @handle_sign_out: Handler for the #GoaIdentityServiceManager::handle-sign-out signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link>.
 */

static void
goa_identity_service_manager_default_init (GoaIdentityServiceManagerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * GoaIdentityServiceManager::handle-exchange-secret-keys:
   * @object: A #GoaIdentityServiceManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_input_key: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Identity-Manager.ExchangeSecretKeys">ExchangeSecretKeys()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call goa_identity_service_manager_complete_exchange_secret_keys() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-exchange-secret-keys",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GoaIdentityServiceManagerIface, handle_exchange_secret_keys),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * GoaIdentityServiceManager::handle-sign-in:
   * @object: A #GoaIdentityServiceManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_identifier: Argument passed by remote caller.
   * @arg_details: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignIn">SignIn()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call goa_identity_service_manager_complete_sign_in() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-sign-in",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GoaIdentityServiceManagerIface, handle_sign_in),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * GoaIdentityServiceManager::handle-sign-out:
   * @object: A #GoaIdentityServiceManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_identity: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignOut">SignOut()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call goa_identity_service_manager_complete_sign_out() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-sign-out",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GoaIdentityServiceManagerIface, handle_sign_out),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

}

typedef GoaIdentityServiceManagerIface GoaIdentityServiceManagerInterface;
G_DEFINE_INTERFACE (GoaIdentityServiceManager, goa_identity_service_manager, G_TYPE_OBJECT);

/**
 * goa_identity_service_manager_call_exchange_secret_keys:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @arg_input_key: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Identity-Manager.ExchangeSecretKeys">ExchangeSecretKeys()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_manager_call_exchange_secret_keys_finish() to get the result of the operation.
 *
 * See goa_identity_service_manager_call_exchange_secret_keys_sync() for the synchronous, blocking version of this method.
 */
void
goa_identity_service_manager_call_exchange_secret_keys (
    GoaIdentityServiceManager *proxy,
    const gchar *arg_input_key,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ExchangeSecretKeys",
    g_variant_new ("(s)",
                   arg_input_key),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * goa_identity_service_manager_call_exchange_secret_keys_finish:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @out_output_key: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_manager_call_exchange_secret_keys().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with goa_identity_service_manager_call_exchange_secret_keys().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
goa_identity_service_manager_call_exchange_secret_keys_finish (
    GoaIdentityServiceManager *proxy,
    gchar **out_output_key,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_output_key);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * goa_identity_service_manager_call_exchange_secret_keys_sync:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @arg_input_key: Argument to pass with the method invocation.
 * @out_output_key: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Identity-Manager.ExchangeSecretKeys">ExchangeSecretKeys()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_manager_call_exchange_secret_keys() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
goa_identity_service_manager_call_exchange_secret_keys_sync (
    GoaIdentityServiceManager *proxy,
    const gchar *arg_input_key,
    gchar **out_output_key,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ExchangeSecretKeys",
    g_variant_new ("(s)",
                   arg_input_key),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_output_key);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * goa_identity_service_manager_call_sign_in:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @arg_identifier: Argument to pass with the method invocation.
 * @arg_details: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignIn">SignIn()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_manager_call_sign_in_finish() to get the result of the operation.
 *
 * See goa_identity_service_manager_call_sign_in_sync() for the synchronous, blocking version of this method.
 */
void
goa_identity_service_manager_call_sign_in (
    GoaIdentityServiceManager *proxy,
    const gchar *arg_identifier,
    GVariant *arg_details,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SignIn",
    g_variant_new ("(s@a{ss})",
                   arg_identifier,
                   arg_details),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * goa_identity_service_manager_call_sign_in_finish:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @out_identity_object_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_manager_call_sign_in().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with goa_identity_service_manager_call_sign_in().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
goa_identity_service_manager_call_sign_in_finish (
    GoaIdentityServiceManager *proxy,
    gchar **out_identity_object_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_identity_object_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * goa_identity_service_manager_call_sign_in_sync:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @arg_identifier: Argument to pass with the method invocation.
 * @arg_details: Argument to pass with the method invocation.
 * @out_identity_object_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignIn">SignIn()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_manager_call_sign_in() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
goa_identity_service_manager_call_sign_in_sync (
    GoaIdentityServiceManager *proxy,
    const gchar *arg_identifier,
    GVariant *arg_details,
    gchar **out_identity_object_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SignIn",
    g_variant_new ("(s@a{ss})",
                   arg_identifier,
                   arg_details),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_identity_object_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * goa_identity_service_manager_call_sign_out:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @arg_identity: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignOut">SignOut()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_manager_call_sign_out_finish() to get the result of the operation.
 *
 * See goa_identity_service_manager_call_sign_out_sync() for the synchronous, blocking version of this method.
 */
void
goa_identity_service_manager_call_sign_out (
    GoaIdentityServiceManager *proxy,
    const gchar *arg_identity,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SignOut",
    g_variant_new ("(s)",
                   arg_identity),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * goa_identity_service_manager_call_sign_out_finish:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_manager_call_sign_out().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with goa_identity_service_manager_call_sign_out().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
goa_identity_service_manager_call_sign_out_finish (
    GoaIdentityServiceManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * goa_identity_service_manager_call_sign_out_sync:
 * @proxy: A #GoaIdentityServiceManagerProxy.
 * @arg_identity: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignOut">SignOut()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_manager_call_sign_out() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
goa_identity_service_manager_call_sign_out_sync (
    GoaIdentityServiceManager *proxy,
    const gchar *arg_identity,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SignOut",
    g_variant_new ("(s)",
                   arg_identity),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * goa_identity_service_manager_complete_exchange_secret_keys:
 * @object: A #GoaIdentityServiceManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @output_key: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Identity-Manager.ExchangeSecretKeys">ExchangeSecretKeys()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
goa_identity_service_manager_complete_exchange_secret_keys (
    GoaIdentityServiceManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *output_key)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   output_key));
}

/**
 * goa_identity_service_manager_complete_sign_in:
 * @object: A #GoaIdentityServiceManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @identity_object_path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignIn">SignIn()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
goa_identity_service_manager_complete_sign_in (
    GoaIdentityServiceManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *identity_object_path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   identity_object_path));
}

/**
 * goa_identity_service_manager_complete_sign_out:
 * @object: A #GoaIdentityServiceManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Identity-Manager.SignOut">SignOut()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
goa_identity_service_manager_complete_sign_out (
    GoaIdentityServiceManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * GoaIdentityServiceManagerProxy:
 *
 * The #GoaIdentityServiceManagerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceManagerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceManagerProxy.
 */

struct _GoaIdentityServiceManagerProxyPrivate
{
  GData *qdata;
};

static void goa_identity_service_manager_proxy_iface_init (GoaIdentityServiceManagerIface *iface);

G_DEFINE_TYPE_WITH_CODE (GoaIdentityServiceManagerProxy, goa_identity_service_manager_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (GOA_IDENTITY_SERVICE_TYPE_MANAGER, goa_identity_service_manager_proxy_iface_init));

static void
goa_identity_service_manager_proxy_finalize (GObject *object)
{
  GoaIdentityServiceManagerProxy *proxy = GOA_IDENTITY_SERVICE_MANAGER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (goa_identity_service_manager_proxy_parent_class)->finalize (object);
}

static void
goa_identity_service_manager_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
goa_identity_service_manager_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
goa_identity_service_manager_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_goa_identity_service_manager_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], GOA_IDENTITY_SERVICE_TYPE_MANAGER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GOA_IDENTITY_SERVICE_TYPE_MANAGER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
goa_identity_service_manager_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  GoaIdentityServiceManagerProxy *proxy = GOA_IDENTITY_SERVICE_MANAGER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_manager_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_manager_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
goa_identity_service_manager_proxy_init (GoaIdentityServiceManagerProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, GOA_IDENTITY_SERVICE_TYPE_MANAGER_PROXY, GoaIdentityServiceManagerProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), goa_identity_service_manager_interface_info ());
}

static void
goa_identity_service_manager_proxy_class_init (GoaIdentityServiceManagerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (GoaIdentityServiceManagerProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = goa_identity_service_manager_proxy_finalize;
  gobject_class->get_property = goa_identity_service_manager_proxy_get_property;
  gobject_class->set_property = goa_identity_service_manager_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = goa_identity_service_manager_proxy_g_signal;
  proxy_class->g_properties_changed = goa_identity_service_manager_proxy_g_properties_changed;

}

static void
goa_identity_service_manager_proxy_iface_init (GoaIdentityServiceManagerIface *iface)
{
}

/**
 * goa_identity_service_manager_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_manager_proxy_new_finish() to get the result of the operation.
 *
 * See goa_identity_service_manager_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
goa_identity_service_manager_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GOA_IDENTITY_SERVICE_TYPE_MANAGER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity.Manager", NULL);
}

/**
 * goa_identity_service_manager_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_manager_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with goa_identity_service_manager_proxy_new().
 *
 * Returns: (transfer full) (type GoaIdentityServiceManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceManager *
goa_identity_service_manager_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_MANAGER (ret);
  else
    return NULL;
}

/**
 * goa_identity_service_manager_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_manager_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GoaIdentityServiceManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceManager *
goa_identity_service_manager_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GOA_IDENTITY_SERVICE_TYPE_MANAGER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity.Manager", NULL);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_MANAGER (ret);
  else
    return NULL;
}


/**
 * goa_identity_service_manager_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like goa_identity_service_manager_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_manager_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See goa_identity_service_manager_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
goa_identity_service_manager_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GOA_IDENTITY_SERVICE_TYPE_MANAGER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity.Manager", NULL);
}

/**
 * goa_identity_service_manager_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_manager_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with goa_identity_service_manager_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type GoaIdentityServiceManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceManager *
goa_identity_service_manager_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_MANAGER (ret);
  else
    return NULL;
}

/**
 * goa_identity_service_manager_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like goa_identity_service_manager_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_manager_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GoaIdentityServiceManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceManager *
goa_identity_service_manager_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GOA_IDENTITY_SERVICE_TYPE_MANAGER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity.Manager", NULL);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_MANAGER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * GoaIdentityServiceManagerSkeleton:
 *
 * The #GoaIdentityServiceManagerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceManagerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceManagerSkeleton.
 */

struct _GoaIdentityServiceManagerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_goa_identity_service_manager_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  GoaIdentityServiceManagerSkeleton *skeleton = GOA_IDENTITY_SERVICE_MANAGER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], GOA_IDENTITY_SERVICE_TYPE_MANAGER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GOA_IDENTITY_SERVICE_TYPE_MANAGER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_goa_identity_service_manager_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  GoaIdentityServiceManagerSkeleton *skeleton = GOA_IDENTITY_SERVICE_MANAGER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_manager_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_goa_identity_service_manager_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  GoaIdentityServiceManagerSkeleton *skeleton = GOA_IDENTITY_SERVICE_MANAGER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_manager_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _goa_identity_service_manager_skeleton_vtable =
{
  _goa_identity_service_manager_skeleton_handle_method_call,
  _goa_identity_service_manager_skeleton_handle_get_property,
  _goa_identity_service_manager_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
goa_identity_service_manager_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return goa_identity_service_manager_interface_info ();
}

static GDBusInterfaceVTable *
goa_identity_service_manager_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_goa_identity_service_manager_skeleton_vtable;
}

static GVariant *
goa_identity_service_manager_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  GoaIdentityServiceManagerSkeleton *skeleton = GOA_IDENTITY_SERVICE_MANAGER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_goa_identity_service_manager_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _goa_identity_service_manager_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _goa_identity_service_manager_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _goa_identity_service_manager_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.Identity.Manager", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
goa_identity_service_manager_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void goa_identity_service_manager_skeleton_iface_init (GoaIdentityServiceManagerIface *iface);
G_DEFINE_TYPE_WITH_CODE (GoaIdentityServiceManagerSkeleton, goa_identity_service_manager_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (GOA_IDENTITY_SERVICE_TYPE_MANAGER, goa_identity_service_manager_skeleton_iface_init));

static void
goa_identity_service_manager_skeleton_finalize (GObject *object)
{
  GoaIdentityServiceManagerSkeleton *skeleton = GOA_IDENTITY_SERVICE_MANAGER_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (goa_identity_service_manager_skeleton_parent_class)->finalize (object);
}

static void
goa_identity_service_manager_skeleton_init (GoaIdentityServiceManagerSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, GOA_IDENTITY_SERVICE_TYPE_MANAGER_SKELETON, GoaIdentityServiceManagerSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
goa_identity_service_manager_skeleton_class_init (GoaIdentityServiceManagerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (GoaIdentityServiceManagerSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = goa_identity_service_manager_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = goa_identity_service_manager_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = goa_identity_service_manager_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = goa_identity_service_manager_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = goa_identity_service_manager_skeleton_dbus_interface_get_vtable;
}

static void
goa_identity_service_manager_skeleton_iface_init (GoaIdentityServiceManagerIface *iface)
{
}

/**
 * goa_identity_service_manager_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link>.
 *
 * Returns: (transfer full) (type GoaIdentityServiceManagerSkeleton): The skeleton object.
 */
GoaIdentityServiceManager *
goa_identity_service_manager_skeleton_new (void)
{
  return GOA_IDENTITY_SERVICE_MANAGER (g_object_new (GOA_IDENTITY_SERVICE_TYPE_MANAGER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.gnome.Identity
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:GoaIdentityServiceIdentity
 * @title: GoaIdentityServiceIdentity
 * @short_description: Generated C code for the org.gnome.Identity D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.gnome.Identity ---- */

static const _ExtendedGDBusPropertyInfo _goa_identity_service_identity_property_info_identifier =
{
  {
    -1,
    (gchar *) "Identifier",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "identifier",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _goa_identity_service_identity_property_info_expiration_timestamp =
{
  {
    -1,
    (gchar *) "ExpirationTimestamp",
    (gchar *) "x",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "expiration-timestamp",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _goa_identity_service_identity_property_info_is_signed_in =
{
  {
    -1,
    (gchar *) "IsSignedIn",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "is-signed-in",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _goa_identity_service_identity_property_info_pointers[] =
{
  &_goa_identity_service_identity_property_info_identifier,
  &_goa_identity_service_identity_property_info_expiration_timestamp,
  &_goa_identity_service_identity_property_info_is_signed_in,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _goa_identity_service_identity_interface_info =
{
  {
    -1,
    (gchar *) "org.gnome.Identity",
    NULL,
    NULL,
    (GDBusPropertyInfo **) &_goa_identity_service_identity_property_info_pointers,
    NULL
  },
  "identity",
};


/**
 * goa_identity_service_identity_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
goa_identity_service_identity_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_goa_identity_service_identity_interface_info.parent_struct;
}

/**
 * goa_identity_service_identity_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #GoaIdentityServiceIdentity interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
goa_identity_service_identity_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "identifier");
  g_object_class_override_property (klass, property_id_begin++, "expiration-timestamp");
  g_object_class_override_property (klass, property_id_begin++, "is-signed-in");
  return property_id_begin - 1;
}



/**
 * GoaIdentityServiceIdentity:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link>.
 */

/**
 * GoaIdentityServiceIdentityIface:
 * @parent_iface: The parent interface.
 * @get_expiration_timestamp: Getter for the #GoaIdentityServiceIdentity:expiration-timestamp property.
 * @get_identifier: Getter for the #GoaIdentityServiceIdentity:identifier property.
 * @get_is_signed_in: Getter for the #GoaIdentityServiceIdentity:is-signed-in property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link>.
 */

static void
goa_identity_service_identity_default_init (GoaIdentityServiceIdentityIface *iface)
{
  /* GObject properties for D-Bus properties: */
  /**
   * GoaIdentityServiceIdentity:identifier:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-gnome-Identity.Identifier">"Identifier"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("identifier", "Identifier", "Identifier", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * GoaIdentityServiceIdentity:expiration-timestamp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-gnome-Identity.ExpirationTimestamp">"ExpirationTimestamp"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int64 ("expiration-timestamp", "ExpirationTimestamp", "ExpirationTimestamp", G_MININT64, G_MAXINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * GoaIdentityServiceIdentity:is-signed-in:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-gnome-Identity.IsSignedIn">"IsSignedIn"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("is-signed-in", "IsSignedIn", "IsSignedIn", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

typedef GoaIdentityServiceIdentityIface GoaIdentityServiceIdentityInterface;
G_DEFINE_INTERFACE (GoaIdentityServiceIdentity, goa_identity_service_identity, G_TYPE_OBJECT);

/**
 * goa_identity_service_identity_get_identifier: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 *
 * Gets the value of the <link linkend="gdbus-property-org-gnome-Identity.Identifier">"Identifier"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use goa_identity_service_identity_dup_identifier() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
goa_identity_service_identity_get_identifier (GoaIdentityServiceIdentity *object)
{
  return GOA_IDENTITY_SERVICE_IDENTITY_GET_IFACE (object)->get_identifier (object);
}

/**
 * goa_identity_service_identity_dup_identifier: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-gnome-Identity.Identifier">"Identifier"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
goa_identity_service_identity_dup_identifier (GoaIdentityServiceIdentity *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "identifier", &value, NULL);
  return value;
}

/**
 * goa_identity_service_identity_set_identifier: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-gnome-Identity.Identifier">"Identifier"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
goa_identity_service_identity_set_identifier (GoaIdentityServiceIdentity *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "identifier", value, NULL);
}

/**
 * goa_identity_service_identity_get_expiration_timestamp: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 *
 * Gets the value of the <link linkend="gdbus-property-org-gnome-Identity.ExpirationTimestamp">"ExpirationTimestamp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint64 
goa_identity_service_identity_get_expiration_timestamp (GoaIdentityServiceIdentity *object)
{
  return GOA_IDENTITY_SERVICE_IDENTITY_GET_IFACE (object)->get_expiration_timestamp (object);
}

/**
 * goa_identity_service_identity_set_expiration_timestamp: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-gnome-Identity.ExpirationTimestamp">"ExpirationTimestamp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
goa_identity_service_identity_set_expiration_timestamp (GoaIdentityServiceIdentity *object, gint64 value)
{
  g_object_set (G_OBJECT (object), "expiration-timestamp", value, NULL);
}

/**
 * goa_identity_service_identity_get_is_signed_in: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 *
 * Gets the value of the <link linkend="gdbus-property-org-gnome-Identity.IsSignedIn">"IsSignedIn"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
goa_identity_service_identity_get_is_signed_in (GoaIdentityServiceIdentity *object)
{
  return GOA_IDENTITY_SERVICE_IDENTITY_GET_IFACE (object)->get_is_signed_in (object);
}

/**
 * goa_identity_service_identity_set_is_signed_in: (skip)
 * @object: A #GoaIdentityServiceIdentity.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-gnome-Identity.IsSignedIn">"IsSignedIn"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
goa_identity_service_identity_set_is_signed_in (GoaIdentityServiceIdentity *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "is-signed-in", value, NULL);
}

/* ------------------------------------------------------------------------ */

/**
 * GoaIdentityServiceIdentityProxy:
 *
 * The #GoaIdentityServiceIdentityProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceIdentityProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceIdentityProxy.
 */

struct _GoaIdentityServiceIdentityProxyPrivate
{
  GData *qdata;
};

static void goa_identity_service_identity_proxy_iface_init (GoaIdentityServiceIdentityIface *iface);

G_DEFINE_TYPE_WITH_CODE (GoaIdentityServiceIdentityProxy, goa_identity_service_identity_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (GOA_IDENTITY_SERVICE_TYPE_IDENTITY, goa_identity_service_identity_proxy_iface_init));

static void
goa_identity_service_identity_proxy_finalize (GObject *object)
{
  GoaIdentityServiceIdentityProxy *proxy = GOA_IDENTITY_SERVICE_IDENTITY_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (goa_identity_service_identity_proxy_parent_class)->finalize (object);
}

static void
goa_identity_service_identity_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _goa_identity_service_identity_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
goa_identity_service_identity_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  error = NULL;
  if (!g_dbus_proxy_call_finish (proxy, res, &error))
    {
      g_warning ("Error setting property `%s' on interface org.gnome.Identity: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
}

static void
goa_identity_service_identity_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _goa_identity_service_identity_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.gnome.Identity", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) goa_identity_service_identity_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
goa_identity_service_identity_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_goa_identity_service_identity_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], GOA_IDENTITY_SERVICE_TYPE_IDENTITY);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GOA_IDENTITY_SERVICE_TYPE_IDENTITY);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
goa_identity_service_identity_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  GoaIdentityServiceIdentityProxy *proxy = GOA_IDENTITY_SERVICE_IDENTITY_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_identity_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_identity_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
goa_identity_service_identity_proxy_get_identifier (GoaIdentityServiceIdentity *object)
{
  GoaIdentityServiceIdentityProxy *proxy = GOA_IDENTITY_SERVICE_IDENTITY_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Identifier");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gint64 
goa_identity_service_identity_proxy_get_expiration_timestamp (GoaIdentityServiceIdentity *object)
{
  GoaIdentityServiceIdentityProxy *proxy = GOA_IDENTITY_SERVICE_IDENTITY_PROXY (object);
  GVariant *variant;
  gint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ExpirationTimestamp");
  if (variant != NULL)
    {
      value = g_variant_get_int64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
goa_identity_service_identity_proxy_get_is_signed_in (GoaIdentityServiceIdentity *object)
{
  GoaIdentityServiceIdentityProxy *proxy = GOA_IDENTITY_SERVICE_IDENTITY_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "IsSignedIn");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
goa_identity_service_identity_proxy_init (GoaIdentityServiceIdentityProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, GOA_IDENTITY_SERVICE_TYPE_IDENTITY_PROXY, GoaIdentityServiceIdentityProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), goa_identity_service_identity_interface_info ());
}

static void
goa_identity_service_identity_proxy_class_init (GoaIdentityServiceIdentityProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (GoaIdentityServiceIdentityProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = goa_identity_service_identity_proxy_finalize;
  gobject_class->get_property = goa_identity_service_identity_proxy_get_property;
  gobject_class->set_property = goa_identity_service_identity_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = goa_identity_service_identity_proxy_g_signal;
  proxy_class->g_properties_changed = goa_identity_service_identity_proxy_g_properties_changed;


  goa_identity_service_identity_override_properties (gobject_class, 1);
}

static void
goa_identity_service_identity_proxy_iface_init (GoaIdentityServiceIdentityIface *iface)
{
  iface->get_identifier = goa_identity_service_identity_proxy_get_identifier;
  iface->get_expiration_timestamp = goa_identity_service_identity_proxy_get_expiration_timestamp;
  iface->get_is_signed_in = goa_identity_service_identity_proxy_get_is_signed_in;
}

/**
 * goa_identity_service_identity_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_identity_proxy_new_finish() to get the result of the operation.
 *
 * See goa_identity_service_identity_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
goa_identity_service_identity_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GOA_IDENTITY_SERVICE_TYPE_IDENTITY_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity", NULL);
}

/**
 * goa_identity_service_identity_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_identity_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with goa_identity_service_identity_proxy_new().
 *
 * Returns: (transfer full) (type GoaIdentityServiceIdentityProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceIdentity *
goa_identity_service_identity_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_IDENTITY (ret);
  else
    return NULL;
}

/**
 * goa_identity_service_identity_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_identity_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GoaIdentityServiceIdentityProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceIdentity *
goa_identity_service_identity_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GOA_IDENTITY_SERVICE_TYPE_IDENTITY_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity", NULL);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_IDENTITY (ret);
  else
    return NULL;
}


/**
 * goa_identity_service_identity_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like goa_identity_service_identity_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_identity_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See goa_identity_service_identity_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
goa_identity_service_identity_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GOA_IDENTITY_SERVICE_TYPE_IDENTITY_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity", NULL);
}

/**
 * goa_identity_service_identity_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_identity_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with goa_identity_service_identity_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type GoaIdentityServiceIdentityProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceIdentity *
goa_identity_service_identity_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_IDENTITY (ret);
  else
    return NULL;
}

/**
 * goa_identity_service_identity_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like goa_identity_service_identity_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_identity_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GoaIdentityServiceIdentityProxy): The constructed proxy object or %NULL if @error is set.
 */
GoaIdentityServiceIdentity *
goa_identity_service_identity_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GOA_IDENTITY_SERVICE_TYPE_IDENTITY_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.Identity", NULL);
  if (ret != NULL)
    return GOA_IDENTITY_SERVICE_IDENTITY (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * GoaIdentityServiceIdentitySkeleton:
 *
 * The #GoaIdentityServiceIdentitySkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceIdentitySkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceIdentitySkeleton.
 */

struct _GoaIdentityServiceIdentitySkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_goa_identity_service_identity_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], GOA_IDENTITY_SERVICE_TYPE_IDENTITY);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GOA_IDENTITY_SERVICE_TYPE_IDENTITY);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_goa_identity_service_identity_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_identity_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_goa_identity_service_identity_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_goa_identity_service_identity_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _goa_identity_service_identity_skeleton_vtable =
{
  _goa_identity_service_identity_skeleton_handle_method_call,
  _goa_identity_service_identity_skeleton_handle_get_property,
  _goa_identity_service_identity_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
goa_identity_service_identity_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return goa_identity_service_identity_interface_info ();
}

static GDBusInterfaceVTable *
goa_identity_service_identity_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_goa_identity_service_identity_skeleton_vtable;
}

static GVariant *
goa_identity_service_identity_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_goa_identity_service_identity_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _goa_identity_service_identity_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _goa_identity_service_identity_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _goa_identity_service_identity_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.Identity", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _goa_identity_service_identity_emit_changed (gpointer user_data);

static void
goa_identity_service_identity_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _goa_identity_service_identity_emit_changed (skeleton);
}

static void goa_identity_service_identity_skeleton_iface_init (GoaIdentityServiceIdentityIface *iface);
G_DEFINE_TYPE_WITH_CODE (GoaIdentityServiceIdentitySkeleton, goa_identity_service_identity_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (GOA_IDENTITY_SERVICE_TYPE_IDENTITY, goa_identity_service_identity_skeleton_iface_init));

static void
goa_identity_service_identity_skeleton_finalize (GObject *object)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  guint n;
  for (n = 0; n < 3; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (goa_identity_service_identity_skeleton_parent_class)->finalize (object);
}

static void
goa_identity_service_identity_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_goa_identity_service_identity_emit_changed (gpointer user_data)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.gnome.Identity",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_goa_identity_service_identity_schedule_emit_changed (GoaIdentityServiceIdentitySkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
goa_identity_service_identity_skeleton_notify (GObject      *object,
  GParamSpec *pspec)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _goa_identity_service_identity_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
goa_identity_service_identity_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _goa_identity_service_identity_schedule_emit_changed (skeleton, _goa_identity_service_identity_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
goa_identity_service_identity_skeleton_init (GoaIdentityServiceIdentitySkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, GOA_IDENTITY_SERVICE_TYPE_IDENTITY_SKELETON, GoaIdentityServiceIdentitySkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 3);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_INT64);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_BOOLEAN);
}

static const gchar *
goa_identity_service_identity_skeleton_get_identifier (GoaIdentityServiceIdentity *object)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint64 
goa_identity_service_identity_skeleton_get_expiration_timestamp (GoaIdentityServiceIdentity *object)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  gint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int64 (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
goa_identity_service_identity_skeleton_get_is_signed_in (GoaIdentityServiceIdentity *object)
{
  GoaIdentityServiceIdentitySkeleton *skeleton = GOA_IDENTITY_SERVICE_IDENTITY_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
goa_identity_service_identity_skeleton_class_init (GoaIdentityServiceIdentitySkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (GoaIdentityServiceIdentitySkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = goa_identity_service_identity_skeleton_finalize;
  gobject_class->get_property = goa_identity_service_identity_skeleton_get_property;
  gobject_class->set_property = goa_identity_service_identity_skeleton_set_property;
  gobject_class->notify       = goa_identity_service_identity_skeleton_notify;


  goa_identity_service_identity_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = goa_identity_service_identity_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = goa_identity_service_identity_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = goa_identity_service_identity_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = goa_identity_service_identity_skeleton_dbus_interface_get_vtable;
}

static void
goa_identity_service_identity_skeleton_iface_init (GoaIdentityServiceIdentityIface *iface)
{
  iface->get_identifier = goa_identity_service_identity_skeleton_get_identifier;
  iface->get_expiration_timestamp = goa_identity_service_identity_skeleton_get_expiration_timestamp;
  iface->get_is_signed_in = goa_identity_service_identity_skeleton_get_is_signed_in;
}

/**
 * goa_identity_service_identity_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link>.
 *
 * Returns: (transfer full) (type GoaIdentityServiceIdentitySkeleton): The skeleton object.
 */
GoaIdentityServiceIdentity *
goa_identity_service_identity_skeleton_new (void)
{
  return GOA_IDENTITY_SERVICE_IDENTITY (g_object_new (GOA_IDENTITY_SERVICE_TYPE_IDENTITY_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for Object, ObjectProxy and ObjectSkeleton
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:GoaIdentityServiceObject
 * @title: GoaIdentityServiceObject
 * @short_description: Specialized GDBusObject types
 *
 * This section contains the #GoaIdentityServiceObject, #GoaIdentityServiceObjectProxy, and #GoaIdentityServiceObjectSkeleton types which make it easier to work with objects implementing generated types for D-Bus interfaces.
 */

/**
 * GoaIdentityServiceObject:
 *
 * The #GoaIdentityServiceObject type is a specialized container of interfaces.
 */

/**
 * GoaIdentityServiceObjectIface:
 * @parent_iface: The parent interface.
 *
 * Virtual table for the #GoaIdentityServiceObject interface.
 */

static void
goa_identity_service_object_default_init (GoaIdentityServiceObjectIface *iface)
{
  /**
   * GoaIdentityServiceObject:manager:
   *
   * The #GoaIdentityServiceManager instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("manager", "manager", "manager", GOA_IDENTITY_SERVICE_TYPE_MANAGER, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

  /**
   * GoaIdentityServiceObject:identity:
   *
   * The #GoaIdentityServiceIdentity instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("identity", "identity", "identity", GOA_IDENTITY_SERVICE_TYPE_IDENTITY, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

}

typedef GoaIdentityServiceObjectIface GoaIdentityServiceObjectInterface;
G_DEFINE_INTERFACE_WITH_CODE (GoaIdentityServiceObject, goa_identity_service_object, G_TYPE_OBJECT, g_type_interface_add_prerequisite (g_define_type_id, G_TYPE_DBUS_OBJECT));

/**
 * goa_identity_service_object_get_manager:
 * @object: A #GoaIdentityServiceObject.
 *
 * Gets the #GoaIdentityServiceManager instance for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link> on @object, if any.
 *
 * Returns: (transfer full): A #GoaIdentityServiceManager that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
GoaIdentityServiceManager *goa_identity_service_object_get_manager (GoaIdentityServiceObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity.Manager");
  if (ret == NULL)
    return NULL;
  return GOA_IDENTITY_SERVICE_MANAGER (ret);
}

/**
 * goa_identity_service_object_get_identity:
 * @object: A #GoaIdentityServiceObject.
 *
 * Gets the #GoaIdentityServiceIdentity instance for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link> on @object, if any.
 *
 * Returns: (transfer full): A #GoaIdentityServiceIdentity that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
GoaIdentityServiceIdentity *goa_identity_service_object_get_identity (GoaIdentityServiceObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity");
  if (ret == NULL)
    return NULL;
  return GOA_IDENTITY_SERVICE_IDENTITY (ret);
}


/**
 * goa_identity_service_object_peek_manager: (skip)
 * @object: A #GoaIdentityServiceObject.
 *
 * Like goa_identity_service_object_get_manager() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #GoaIdentityServiceManager or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
GoaIdentityServiceManager *goa_identity_service_object_peek_manager (GoaIdentityServiceObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity.Manager");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return GOA_IDENTITY_SERVICE_MANAGER (ret);
}

/**
 * goa_identity_service_object_peek_identity: (skip)
 * @object: A #GoaIdentityServiceObject.
 *
 * Like goa_identity_service_object_get_identity() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #GoaIdentityServiceIdentity or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
GoaIdentityServiceIdentity *goa_identity_service_object_peek_identity (GoaIdentityServiceObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return GOA_IDENTITY_SERVICE_IDENTITY (ret);
}


static void
goa_identity_service_object_notify (GDBusObject *object, GDBusInterface *interface)
{
  g_object_notify (G_OBJECT (object), ((_ExtendedGDBusInterfaceInfo *) g_dbus_interface_get_info (interface))->hyphen_name);
}

/**
 * GoaIdentityServiceObjectProxy:
 *
 * The #GoaIdentityServiceObjectProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceObjectProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceObjectProxy.
 */

static void
goa_identity_service_object_proxy__goa_identity_service_object_iface_init (GoaIdentityServiceObjectIface *iface)
{
}

static void
goa_identity_service_object_proxy__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = goa_identity_service_object_notify;
  iface->interface_removed = goa_identity_service_object_notify;
}


G_DEFINE_TYPE_WITH_CODE (GoaIdentityServiceObjectProxy, goa_identity_service_object_proxy, G_TYPE_DBUS_OBJECT_PROXY,
                         G_IMPLEMENT_INTERFACE (GOA_IDENTITY_SERVICE_TYPE_OBJECT, goa_identity_service_object_proxy__goa_identity_service_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, goa_identity_service_object_proxy__g_dbus_object_iface_init));

static void
goa_identity_service_object_proxy_init (GoaIdentityServiceObjectProxy *object)
{
}

static void
goa_identity_service_object_proxy_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
}

static void
goa_identity_service_object_proxy_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  GoaIdentityServiceObjectProxy *object = GOA_IDENTITY_SERVICE_OBJECT_PROXY (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity.Manager");
      g_value_take_object (value, interface);
      break;

    case 2:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
goa_identity_service_object_proxy_class_init (GoaIdentityServiceObjectProxyClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = goa_identity_service_object_proxy_set_property;
  gobject_class->get_property = goa_identity_service_object_proxy_get_property;

  g_object_class_override_property (gobject_class, 1, "manager");
  g_object_class_override_property (gobject_class, 2, "identity");
}

/**
 * goa_identity_service_object_proxy_new:
 * @connection: A #GDBusConnection.
 * @object_path: An object path.
 *
 * Creates a new proxy object.
 *
 * Returns: (transfer full): The proxy object.
 */
GoaIdentityServiceObjectProxy *
goa_identity_service_object_proxy_new (GDBusConnection *connection,
  const gchar *object_path)
{
  g_return_val_if_fail (G_IS_DBUS_CONNECTION (connection), NULL);
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return GOA_IDENTITY_SERVICE_OBJECT_PROXY (g_object_new (GOA_IDENTITY_SERVICE_TYPE_OBJECT_PROXY, "g-connection", connection, "g-object-path", object_path, NULL));
}

/**
 * GoaIdentityServiceObjectSkeleton:
 *
 * The #GoaIdentityServiceObjectSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceObjectSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceObjectSkeleton.
 */

static void
goa_identity_service_object_skeleton__goa_identity_service_object_iface_init (GoaIdentityServiceObjectIface *iface)
{
}


static void
goa_identity_service_object_skeleton__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = goa_identity_service_object_notify;
  iface->interface_removed = goa_identity_service_object_notify;
}

G_DEFINE_TYPE_WITH_CODE (GoaIdentityServiceObjectSkeleton, goa_identity_service_object_skeleton, G_TYPE_DBUS_OBJECT_SKELETON,
                         G_IMPLEMENT_INTERFACE (GOA_IDENTITY_SERVICE_TYPE_OBJECT, goa_identity_service_object_skeleton__goa_identity_service_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, goa_identity_service_object_skeleton__g_dbus_object_iface_init));

static void
goa_identity_service_object_skeleton_init (GoaIdentityServiceObjectSkeleton *object)
{
}

static void
goa_identity_service_object_skeleton_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  GoaIdentityServiceObjectSkeleton *object = GOA_IDENTITY_SERVICE_OBJECT_SKELETON (gobject);
  GDBusInterfaceSkeleton *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (GOA_IDENTITY_SERVICE_IS_MANAGER (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.gnome.Identity.Manager");
        }
      break;

    case 2:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (GOA_IDENTITY_SERVICE_IS_IDENTITY (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.gnome.Identity");
        }
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
goa_identity_service_object_skeleton_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  GoaIdentityServiceObjectSkeleton *object = GOA_IDENTITY_SERVICE_OBJECT_SKELETON (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity.Manager");
      g_value_take_object (value, interface);
      break;

    case 2:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.Identity");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
goa_identity_service_object_skeleton_class_init (GoaIdentityServiceObjectSkeletonClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = goa_identity_service_object_skeleton_set_property;
  gobject_class->get_property = goa_identity_service_object_skeleton_get_property;

  g_object_class_override_property (gobject_class, 1, "manager");
  g_object_class_override_property (gobject_class, 2, "identity");
}

/**
 * goa_identity_service_object_skeleton_new:
 * @object_path: An object path.
 *
 * Creates a new skeleton object.
 *
 * Returns: (transfer full): The skeleton object.
 */
GoaIdentityServiceObjectSkeleton *
goa_identity_service_object_skeleton_new (const gchar *object_path)
{
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return GOA_IDENTITY_SERVICE_OBJECT_SKELETON (g_object_new (GOA_IDENTITY_SERVICE_TYPE_OBJECT_SKELETON, "g-object-path", object_path, NULL));
}

/**
 * goa_identity_service_object_skeleton_set_manager:
 * @object: A #GoaIdentityServiceObjectSkeleton.
 * @interface_: (allow-none): A #GoaIdentityServiceManager or %NULL to clear the interface.
 *
 * Sets the #GoaIdentityServiceManager instance for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity-Manager.top_of_page">org.gnome.Identity.Manager</link> on @object.
 */
void goa_identity_service_object_skeleton_set_manager (GoaIdentityServiceObjectSkeleton *object, GoaIdentityServiceManager *interface_)
{
  g_object_set (G_OBJECT (object), "manager", interface_, NULL);
}

/**
 * goa_identity_service_object_skeleton_set_identity:
 * @object: A #GoaIdentityServiceObjectSkeleton.
 * @interface_: (allow-none): A #GoaIdentityServiceIdentity or %NULL to clear the interface.
 *
 * Sets the #GoaIdentityServiceIdentity instance for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Identity.top_of_page">org.gnome.Identity</link> on @object.
 */
void goa_identity_service_object_skeleton_set_identity (GoaIdentityServiceObjectSkeleton *object, GoaIdentityServiceIdentity *interface_)
{
  g_object_set (G_OBJECT (object), "identity", interface_, NULL);
}


/* ------------------------------------------------------------------------
 * Code for ObjectManager client
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:GoaIdentityServiceObjectManagerClient
 * @title: GoaIdentityServiceObjectManagerClient
 * @short_description: Generated GDBusObjectManagerClient type
 *
 * This section contains a #GDBusObjectManagerClient that uses goa_identity_service_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc.
 */

/**
 * GoaIdentityServiceObjectManagerClient:
 *
 * The #GoaIdentityServiceObjectManagerClient structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GoaIdentityServiceObjectManagerClientClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GoaIdentityServiceObjectManagerClient.
 */

G_DEFINE_TYPE (GoaIdentityServiceObjectManagerClient, goa_identity_service_object_manager_client, G_TYPE_DBUS_OBJECT_MANAGER_CLIENT);

static void
goa_identity_service_object_manager_client_init (GoaIdentityServiceObjectManagerClient *manager)
{
}

static void
goa_identity_service_object_manager_client_class_init (GoaIdentityServiceObjectManagerClientClass *klass)
{
}

/**
 * goa_identity_service_object_manager_client_get_proxy_type:
 * @manager: A #GDBusObjectManagerClient.
 * @object_path: The object path of the remote object (unused).
 * @interface_name: (allow-none): Interface name of the remote object or %NULL to get the object proxy #GType.
 * @user_data: User data (unused).
 *
 * A #GDBusProxyTypeFunc that maps @interface_name to the generated #GDBusObjectProxy<!-- -->- and #GDBusProxy<!-- -->-derived types.
 *
 * Returns: A #GDBusProxy<!-- -->-derived #GType if @interface_name is not %NULL, otherwise the #GType for #GoaIdentityServiceObjectProxy.
 */
GType
goa_identity_service_object_manager_client_get_proxy_type (GDBusObjectManagerClient *manager, const gchar *object_path, const gchar *interface_name, gpointer user_data)
{
  static gsize once_init_value = 0;
  static GHashTable *lookup_hash;
  GType ret;

  if (interface_name == NULL)
    return GOA_IDENTITY_SERVICE_TYPE_OBJECT_PROXY;
  if (g_once_init_enter (&once_init_value))
    {
      lookup_hash = g_hash_table_new (g_str_hash, g_str_equal);
      g_hash_table_insert (lookup_hash, (gpointer) "org.gnome.Identity.Manager", GSIZE_TO_POINTER (GOA_IDENTITY_SERVICE_TYPE_MANAGER_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.gnome.Identity", GSIZE_TO_POINTER (GOA_IDENTITY_SERVICE_TYPE_IDENTITY_PROXY));
      g_once_init_leave (&once_init_value, 1);
    }
  ret = (GType) GPOINTER_TO_SIZE (g_hash_table_lookup (lookup_hash, interface_name));
  if (ret == (GType) 0)
    ret = G_TYPE_DBUS_PROXY;
  return ret;
}

/**
 * goa_identity_service_object_manager_client_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates #GDBusObjectManagerClient using goa_identity_service_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_object_manager_client_new_finish() to get the result of the operation.
 *
 * See goa_identity_service_object_manager_client_new_sync() for the synchronous, blocking version of this constructor.
 */
void
goa_identity_service_object_manager_client_new (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (GOA_IDENTITY_SERVICE_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", goa_identity_service_object_manager_client_get_proxy_type, NULL);
}

/**
 * goa_identity_service_object_manager_client_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_object_manager_client_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with goa_identity_service_object_manager_client_new().
 *
 * Returns: (transfer full) (type GoaIdentityServiceObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
goa_identity_service_object_manager_client_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * goa_identity_service_object_manager_client_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates #GDBusObjectManagerClient using goa_identity_service_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_object_manager_client_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GoaIdentityServiceObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
goa_identity_service_object_manager_client_new_sync (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (GOA_IDENTITY_SERVICE_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", goa_identity_service_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


/**
 * goa_identity_service_object_manager_client_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like goa_identity_service_object_manager_client_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call goa_identity_service_object_manager_client_new_for_bus_finish() to get the result of the operation.
 *
 * See goa_identity_service_object_manager_client_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
goa_identity_service_object_manager_client_new_for_bus (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (GOA_IDENTITY_SERVICE_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", goa_identity_service_object_manager_client_get_proxy_type, NULL);
}

/**
 * goa_identity_service_object_manager_client_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to goa_identity_service_object_manager_client_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with goa_identity_service_object_manager_client_new_for_bus().
 *
 * Returns: (transfer full) (type GoaIdentityServiceObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
goa_identity_service_object_manager_client_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * goa_identity_service_object_manager_client_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like goa_identity_service_object_manager_client_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See goa_identity_service_object_manager_client_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GoaIdentityServiceObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
goa_identity_service_object_manager_client_new_for_bus_sync (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (GOA_IDENTITY_SERVICE_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", goa_identity_service_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


